name: Archived RenderGraph Queue Synchronization Migration

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Queue synchronization migration completed and archived."
