name: Archived D3D11 RHI Real Triangle Validation

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "D3D11 RHI real triangle validation completed and archived."
