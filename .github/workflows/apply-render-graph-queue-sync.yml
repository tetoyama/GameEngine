name: Archived RenderGraph Pass Culling Migration

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "RenderGraph Pass Culling migration completed and archived."
