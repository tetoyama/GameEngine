name: Asset Cache Invalidation Migration

on:
  push:
    branches:
      - refactor/ecs-scheduler-foundation
    paths:
      - .github/workflows/asset-cache-invalidation-migration.yml
      - Tools/Migrations/fix_asset_cache_invalidation.py

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout migration branch
        uses: actions/checkout@v4
        with:
          ref: refactor/ecs-scheduler-foundation
          fetch-depth: 0

      - name: Apply deferred asset cache invalidation
        run: python Tools/Migrations/fix_asset_cache_invalidation.py

      - name: Remove temporary migration files
        run: |
          rm Tools/Migrations/fix_asset_cache_invalidation.py
          rm .github/workflows/asset-cache-invalidation-migration.yml

      - name: Commit migration result
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Source/GameApplication/Engine/Editor/UI/AssetsBrowser.h
          git add Source/GameApplication/Engine/Editor/UI/AssetsBrowser.cpp
          git add Tools/Migrations/fix_asset_cache_invalidation.py
          git add .github/workflows/asset-cache-invalidation-migration.yml
          git diff --cached --exit-code && exit 0
          git commit -m "fix: defer asset cache invalidation and lazy-load tree [skip ci]"
          git push origin HEAD:refactor/ecs-scheduler-foundation
