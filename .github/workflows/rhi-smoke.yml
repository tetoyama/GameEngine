# RHI sources are compiled on pull requests and branch pushes.
# Executable smoke tests run only when manually dispatched.
name: RHI Smoke Test

on:
  workflow_dispatch:
  pull_request:
    branches:
      - master
  push:
    branches:
      - refactor/ecs-scheduler-foundation

concurrency:
  group: rhi-smoke-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  null-rhi-smoke-x64:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          show-progress: false
      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64
      - name: Compile Null RHI smoke source
        id: rhi_build
        continue-on-error: true
        shell: cmd
        run: >-
          cl /nologo /std:c++20 /EHsc /W4 /MTd
          /I Source
          /I Source/GameApplication
          Tests/RHISmokeTest.cpp
          /Fe:rhi-smoke.exe
          > rhi-smoke-build.log 2>&1
      - name: Upload Null RHI compiler diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: null-rhi-smoke-build-log
          path: rhi-smoke-build.log
          if-no-files-found: error
      - name: Preserve Null RHI compile result
        if: steps.rhi_build.outcome == 'failure'
        run: cmd /c exit 1
      - name: Run Null RHI smoke test
        if: github.event_name == 'workflow_dispatch'
        run: .\rhi-smoke.exe

  render-graph-barrier-smoke-x64:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          show-progress: false
      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64
      - name: Compile RenderGraph barrier smoke source
        id: render_graph_build
        continue-on-error: true
        shell: cmd
        run: >-
          cl /nologo /std:c++20 /EHsc /W4 /MTd
          /I Source
          /I Source/GameApplication
          Tests/RHIRenderGraphBarrierSmokeTest.cpp
          /Fe:rhi-render-graph-barrier-smoke.exe
          > rhi-render-graph-barrier-build.log 2>&1
      - name: Upload RenderGraph compiler diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rhi-render-graph-barrier-build-log
          path: rhi-render-graph-barrier-build.log
          if-no-files-found: error
      - name: Preserve RenderGraph compile result
        if: steps.render_graph_build.outcome == 'failure'
        run: cmd /c exit 1
      - name: Run RenderGraph barrier smoke test
        if: github.event_name == 'workflow_dispatch'
        run: .\rhi-render-graph-barrier-smoke.exe

  d3d11-rhi-backend-smoke-x64:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          show-progress: false
      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64
      - name: Compile Direct3D11 RHI backend smoke source
        id: d3d11_build
        continue-on-error: true
        shell: cmd
        run: >-
          cl /nologo /std:c++20 /EHsc /W4 /MTd
          /I Source
          /I Source/GameApplication
          Tests/D3D11RHIBackendSmokeTest.cpp
          d3d11.lib dxgi.lib
          /Fe:d3d11-rhi-backend-smoke.exe
          > d3d11-rhi-backend-build.log 2>&1
      - name: Upload Direct3D11 compiler diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: d3d11-rhi-backend-build-log
          path: d3d11-rhi-backend-build.log
          if-no-files-found: error
      - name: Preserve Direct3D11 compile result
        if: steps.d3d11_build.outcome == 'failure'
        run: cmd /c exit 1
      - name: Run Direct3D11 RHI backend smoke test
        if: github.event_name == 'workflow_dispatch'
        run: .\d3d11-rhi-backend-smoke.exe

  swap-chain-contract-smoke-x64:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          show-progress: false
      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64
      - name: Compile SwapChain contract smoke source
        id: swap_chain_build
        continue-on-error: true
        shell: cmd
        run: >-
          cl /nologo /std:c++20 /EHsc /W4 /MTd
          /I Source
          /I Source/GameApplication
          Tests/RHISwapChainContractSmokeTest.cpp
          d3d11.lib dxgi.lib user32.lib
          /Fe:rhi-swap-chain-contract-smoke.exe
          > rhi-swap-chain-contract-build.log 2>&1
      - name: Upload SwapChain compiler diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rhi-swap-chain-contract-build-log
          path: rhi-swap-chain-contract-build.log
          if-no-files-found: error
      - name: Preserve SwapChain contract compile result
        if: steps.swap_chain_build.outcome == 'failure'
        run: cmd /c exit 1
      - name: Run SwapChain contract smoke test
        if: github.event_name == 'workflow_dispatch'
        run: .\rhi-swap-chain-contract-smoke.exe
