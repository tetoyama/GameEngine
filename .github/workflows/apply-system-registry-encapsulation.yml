name: Apply SystemRegistry Encapsulation

on:
  push:
    branches:
      - refactor/ecs-scheduler-foundation
    paths:
      - Tools/Migration/encapsulate_system_registry.py
      - .github/workflows/apply-system-registry-encapsulation.yml

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/ecs-scheduler-foundation
          fetch-depth: 0
      - run: python Tools/Migration/encapsulate_system_registry.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm Tools/Migration/encapsulate_system_registry.py
          git rm .github/workflows/apply-system-registry-encapsulation.yml
          git add -A
          git commit -m "refactor: encapsulate SystemRegistry ownership"
          git push --force origin HEAD:system-registry-encapsulation-output
